description = 'KrakenSDR processing modules'
ext.details = 'Kraken process testing'
version = '0.1'


dependencies {
  implementation 'org.sensorhub:sensorhub-core:' + oshCoreVersion
  implementation 'org.sensorhub:sensorhub-service-swe:' + oshCoreVersion

//  testImplementation project(':sensorhub-driver-krakenSDR')
}

// add info to OSGi manifest
osgi {
  manifest {
    attributes('Bundle-Vendor': 'Sensia Software LLC')
    attributes('Bundle-Activator': 'org.sensorhub.impl.process.video.Activator')
  }
}

// add info to maven pom
ext.pom >>= {
  developers {
    developer {
      id 'billbrown'
      name 'Bill Brown'
      organization 'Sensia Software LLC'
      organizationUrl 'http://www.sensiasoftware.com' 
    }
  }
}

